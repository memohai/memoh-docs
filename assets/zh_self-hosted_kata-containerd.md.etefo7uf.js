import{_ as i,o as a,c as e,a2 as t}from"./chunks/framework.BZO4qERu.js";const c=JSON.parse('{"title":"Containerd + Kata","description":"","frontmatter":{},"headers":[],"relativePath":"zh/self-hosted/kata-containerd.md","filePath":"zh/self-hosted/kata-containerd.md","lastUpdated":1781675678000}'),n={name:"zh/self-hosted/kata-containerd.md"};function p(l,s,h,o,d,k){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="containerd-kata" tabindex="-1">Containerd + Kata <a class="header-anchor" href="#containerd-kata" aria-label="Permalink to &quot;Containerd + Kata&quot;">​</a></h1><p>Memoh 可以在 Linux/KVM 主机上通过 containerd 的 Kata runtime 运行机器人 workspace。Memoh 的 workspace API 和生命周期模型保持不变，但 containerd 会用 <code>io.containerd.kata.v2</code> 创建每个 workspace。</p><p>这条路径只支持 Linux。macOS 或 Docker Desktop 主机可以验证 compose 语法和 runc 回归，但不能证明 Kata runtime 本身可用。</p><h2 id="能力范围" tabindex="-1">能力范围 <a class="header-anchor" href="#能力范围" aria-label="Permalink to &quot;能力范围&quot;">​</a></h2><ul><li><code>[container].backend = &quot;containerd&quot;</code> 仍然是 workspace backend。</li><li><code>[containerd].runtime_type = &quot;io.containerd.kata.v2&quot;</code> 会为机器人 workspace 容器选择 Kata。</li><li><code>server-kata</code> image target 使用 Debian/glibc runtime，因为宿主机 Kata shim 通常链接 glibc。</li><li>compose overrides 会把 <code>/dev/kvm</code>、宿主机 Kata shim、Kata 配置和 Kata runtime assets 挂进 Memoh server 容器。</li></ul><p>Memoh 仍然通过 containerd snapshot 驱动 Kata workspace。CPU 和内存限制是 hard limit。存储限制会先保存并展示为 soft limit，直到 VM disk quota 或 block-device quota 支持完成。</p><p>Kata workspace 使用 bridge TCP listener（<code>BRIDGE_TCP_ADDR=:9090</code>），不使用普通 containerd workspace 的 Unix socket bridge。Unix socket 可能会通过 Kata shared filesystem 出现在宿主机上，但它不是可跨 guest VM 边界使用的连接端点。Memoh 会把 Kata bridge 流量路由到 workspace 的 CNI IP，并且 bridge gRPC dial 会禁用 HTTP proxy，避免 server 容器上的 proxy 设置拦截私有 workspace 地址。</p><p>共享部署请启用 <code>[bridge_tls].mode = &quot;strict&quot;</code>。默认的 <code>disabled</code> 模式方便本地 安装，但不能保护共享 Kata/CNI 网络免受 workspace-to-workspace bridge 流量影响。 在多租户 Kata 部署中，除非你的网络层已经独立阻断 workspace 之间的 TCP 访问， 否则 strict bridge mTLS 是必需的隔离层。</p><h2 id="bridge-mtls-材料" tabindex="-1">Bridge mTLS 材料 <a class="header-anchor" href="#bridge-mtls-材料" aria-label="Permalink to &quot;Bridge mTLS 材料&quot;">​</a></h2><p>使用仓库工具生成 strict bridge mTLS 材料，不要手写 OpenSSL 命令：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">INSTANCE_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uuidgen</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tr</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;[:upper:]&#39; &#39;[:lower:]&#39;)&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/gen-bridge-mtls.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -instance-id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$INSTANCE_ID</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/memoh/bridge-mtls</span></span></code></pre></div><p>等价的 <code>mise</code> 命令是：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bridge:mtls:gen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -instance-id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$INSTANCE_ID</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/memoh/bridge-mtls</span></span></code></pre></div><p>生成器会创建两个独立 CA，为 Memoh server 签发一个 <code>ClientAuth</code> 证书，为 workspace bridge 签发一个 <code>ServerAuth</code> 证书，只写入必需的 leaf keys 和 CA bundles，并丢弃两个 CA private keys。它会打印类似下面的配置片段：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">instance_id = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;11111111-1111-1111-1111-111111111111&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bridge_tls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;strict&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">server_dir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/opt/memoh/bridge-mtls/server&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bridge_dir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/opt/memoh/bridge-mtls/bridge&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">server_name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span></code></pre></div><p><code>server_dir</code> 只由 Memoh server 读取，包含：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>server-client.crt</span></span>
<span class="line"><span>server-client.key</span></span>
<span class="line"><span>bridge-server-ca.crt</span></span></code></pre></div><p><code>bridge_dir</code> 会以只读方式挂载到 workspace 容器，并且只能包含：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bridge-server.crt</span></span>
<span class="line"><span>bridge-server.key</span></span>
<span class="line"><span>server-client-ca.crt</span></span></code></pre></div><p>不要把 <code>server_dir</code> 复用成 <code>bridge_dir</code>，也不要把 <code>server-client.crt</code> 或 <code>server-client.key</code> 复制到 <code>bridge_dir</code>。Strict mode 会在向 workspace 挂载任何 内容之前拒绝共享目录、指向同一位置的 symlink 目录、缺失材料，或 <code>bridge_dir</code> 中出现的非预期文件。</p><p>如果你在现有部署上启用 strict bridge mTLS，需要重建或重启所有已有 workspace 容器。旧容器没有 TLS 材料挂载，也没有 <code>BRIDGE_TLS_*</code> 环境变量；一旦 server 要求 mTLS，它们会 fail closed。</p><h2 id="宿主机要求" tabindex="-1">宿主机要求 <a class="header-anchor" href="#宿主机要求" aria-label="Permalink to &quot;宿主机要求&quot;">​</a></h2><ul><li>Linux 主机，并且 <code>/dev/kvm</code> 可用。</li><li>如果宿主机本身也是 VM，需要开启 nested virtualization。</li><li>Docker 和 Docker Compose v2。</li><li>宿主机已安装 Kata Containers。</li><li>宿主机需要安装 <code>curl</code> 和 <code>jq</code>，供 API verifier 使用。</li></ul><p>默认宿主机路径：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMOH_KATA_SHIM_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/opt/kata/bin/containerd-shim-kata-v2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMOH_KATA_CONFIG_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/etc/kata-containers</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMOH_KATA_SHARE_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/usr/share/kata-containers</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMOH_KATA_OPT_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/opt/kata</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMOH_KATA_SYSLOG_SOCKET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/run/systemd/journal/dev-log</span></span></code></pre></div><p>如果你的 Kata 安装路径不同，在运行 dev 或 production compose 命令前 export 这些变量。syslog socket 会挂载到 server 容器内的 <code>/dev/log</code>，让 Kata shim 可以初始化日志。在非 systemd 主机上，把 <code>MEMOH_KATA_SYSLOG_SOCKET</code> 设置为宿主机 实际的 syslog socket 路径。</p><p>Dockerized Memoh server 会为 workspace 容器运行一个嵌套 containerd。因此 Kata compose overrides 会给 server service 设置 <code>cgroup: host</code> 和 <code>shm_size: 1gb</code>。 宿主机 cgroup namespace 允许嵌套 Kata runtime 创建 sandbox cgroup controllers； 更大的 shared-memory segment 可以避免 Docker 默认 <code>/dev/shm</code> 太小导致的 QEMU/KVM 启动失败。</p><p>如果 Linux/KVM 主机在 Docker image build 阶段需要代理，运行 Kata compose tasks 之前先设置 build proxy 变量：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMOH_KATA_BUILD_HTTP_PROXY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://172.17.0.1:7890</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMOH_KATA_BUILD_HTTPS_PROXY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://172.17.0.1:7890</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMOH_KATA_BUILD_NO_PROXY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">127.0.0.1,localhost</span></span></code></pre></div><p>这里要使用 Docker build 容器能访问到的宿主机地址。在 Linux 上，Dockerfile <code>RUN</code> 步骤里的 <code>localhost</code> 指的是 build 容器自己，不是宿主机。</p><h2 id="development-stack" tabindex="-1">Development Stack <a class="header-anchor" href="#development-stack" aria-label="Permalink to &quot;Development Stack&quot;">​</a></h2><p>在专用 Linux/KVM development host 上运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kata:runner</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev:kata</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mise</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev:kata:status</span></span></code></pre></div><p><code>kata:runner</code> 是面向 runner 或 development host 的轻量 readiness check。它会写入 <code>tmp/kata-evidence/environment.txt</code>，验证 Docker 和 Docker Compose 可用，然后在 启动任何 Memoh stack 之前检查 Linux、<code>/dev/kvm</code>、Kata shim、Kata 配置和 Kata runtime asset 目录。</p><p><code>dev:kata:status</code> 是面向当前 dev server 的轻量诊断命令。当 <code>http://127.0.0.1:18082</code> 已经打开，并且你需要确认后端 server 是 Kata dev stack 还是普通 dev stack 时使用。它会检查 <code>/ping</code>、<code>memoh-dev-server</code> 容器的 <code>CONFIG_PATH</code>、<code>/dev/kvm</code> 以及 Kata shim/config mounts。</p><p>Kata 下预期看到 <code>container_backend = &quot;containerd&quot;</code>。真正证明 workspace runtime 是 Kata 的值，是机器人 workspace 上的 <code>runtime_backend = &quot;io.containerd.kata.v2&quot;</code>， 或 <code>ctr containers info</code> 输出中的 <code>Runtime.Name = &quot;io.containerd.kata.v2&quot;</code>。</p><h2 id="production-compose" tabindex="-1">Production Compose <a class="header-anchor" href="#production-compose" aria-label="Permalink to &quot;Production Compose&quot;">​</a></h2><p>请在专用 Linux/KVM 主机上使用，因为根 compose 文件使用固定容器名，例如 <code>memoh-server</code> 和 <code>memoh-postgres</code>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.yml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.kata.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span></span></code></pre></div><p>手动 production 部署时，先复制并编辑 Kata 配置：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> conf/app.kata.docker.toml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config.kata.toml</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Change admin password, JWT secret, and database password.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMOH_CONFIG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./config.kata.toml</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.yml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.kata.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>启动后，创建或重建一个机器人 workspace，并确认 workspace runtime 是 <code>io.containerd.kata.v2</code>。</p><p>也可以在 server 的 containerd 环境里直接运行一个 Kata runtime smoke test：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ctr</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> default</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --runtime</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> io.containerd.kata.v2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --snapshotter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> overlayfs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  docker.io/library/alpine:3.22</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  memoh-kata-smoke</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  /bin/sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -lc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;printf &quot;runtime-smoke-ok\\n&quot;&#39;</span></span></code></pre></div><p>通过 <code>ctr -n default containers info workspace-&lt;bot-id&gt;</code> 看到的预期 container runtime name 是 <code>io.containerd.kata.v2</code>。</p><h2 id="github-actions-runner" tabindex="-1">GitHub Actions Runner <a class="header-anchor" href="#github-actions-runner" aria-label="Permalink to &quot;GitHub Actions Runner&quot;">​</a></h2><p>用这些 labels 注册 runner：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>self-hosted, linux, x64, kvm, kata</span></span></code></pre></div><p>在 Linux/KVM 主机上，这个命令可以运行 readiness preflight，并生成 runner registration script。生成的脚本会添加必需的 <code>kvm,kata</code> labels，但不会把短期有效 的 GitHub registration token 写入文件：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/prepare-kata-github-runner.sh</span></span></code></pre></div><p>设置 <code>MEMOH_KATA_RUNNER_NAME</code>、<code>MEMOH_KATA_RUNNER_DIR</code> 或 <code>MEMOH_KATA_RUNNER_SCRIPT</code> 可以覆盖生成的 runner 名称、安装目录或输出脚本。 等价的 <code>mise</code> 命令是 <code>mise run kata:github:runner</code>。生成的注册脚本会在注册前 重新检查 Linux、x86_64/amd64、<code>/dev/kvm</code>、Docker Compose 和 Kata shim/config 路径，所以复制出去的脚本不会静默地把错误主机注册为带有 <code>kvm,kata</code> labels 的 runner。</p><p>如果要在不启动 Memoh stack 的情况下检查新注册的 runner，可以手动运行 workflow， 并设置 <code>run_runner_readiness=true</code>。这只会运行 <code>scripts/check-kata-runner-ready.sh</code>，并上传包含环境摘要的 <code>kata-runner-readiness</code> artifact。</p><p>当带有必需 labels 的 runner 注册完成后，可以用这个命令从 PR branch dispatch readiness workflow，等待它结束，并审计 PR checks：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/run-kata-github-e2e.sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pr-numbe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>等价的 <code>mise</code> 命令是 <code>mise run kata:github:readiness -- &lt;pr-number&gt;</code>。因为 GitHub 要求 <code>workflow_dispatch</code> workflows 先注册后才能手动运行，这个命令会在 dispatch 前检查 workflow 是否可用。</p><p>如果要审计某个 PR head 是否已经完成 runner readiness verification，运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/audit-kata-github-verification.sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pr-numbe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>对应 task 是 <code>mise run kata:github -- &lt;pr-number&gt;</code>。</p><h2 id="排障" tabindex="-1">排障 <a class="header-anchor" href="#排障" aria-label="Permalink to &quot;排障&quot;">​</a></h2><ul><li><code>Kata validation requires a Linux host with KVM</code>：在带 KVM 的 Linux 主机上跑 E2E。Docker Desktop 不够。</li><li><code>/dev/kvm is missing</code>：启用 KVM 或 nested virtualization，然后确认 Docker 能把 <code>/dev/kvm</code> 传入容器。</li><li><code>Kata shim not found</code>：把 <code>MEMOH_KATA_SHIM_PATH</code> 设置为宿主机 <code>containerd-shim-kata-v2</code> 路径。</li><li><code>configuration.toml</code> 中引用的路径缺失：挂载对应 Kata assets，或把 <code>MEMOH_KATA_SHARE_DIR</code> / <code>MEMOH_KATA_OPT_DIR</code> 设置为正确的宿主机路径。</li><li><code>ctr containers info</code> 里 runtime mismatch：确认 server config 使用 <code>runtime_type = &quot;io.containerd.kata.v2&quot;</code>，并且包含 Kata compose override。</li><li>直接 Kata smoke test 通过，但机器人 workspace 无法启动：检查 server 日志中是否 有 Kata shim、cgroup、<code>/dev/log</code>、<code>/dev/kvm</code> 或 bridge 连接错误。</li></ul>`,60)])])}const g=i(n,[["render",p]]);export{c as __pageData,g as default};
