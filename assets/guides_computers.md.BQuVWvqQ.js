import{_ as t,o,c as a,a2 as n}from"./chunks/framework.BZO4qERu.js";const u=JSON.parse('{"title":"Computers (Remote Runtimes)","description":"","frontmatter":{},"headers":[],"relativePath":"guides/computers.md","filePath":"guides/computers.md","lastUpdated":1785920317000}'),s={name:"guides/computers.md"};function r(i,e,c,l,h,d){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="computers-remote-runtimes" tabindex="-1">Computers (Remote Runtimes) <a class="header-anchor" href="#computers-remote-runtimes" aria-label="Permalink to &quot;Computers (Remote Runtimes)&quot;">​</a></h1><p>Memoh can connect your own computers to a deployment so bots can work on them: read and write files, and run commands. The Web UI calls these <strong>Computers</strong>; a bot picks between them as <strong>Work locations</strong>.</p><p>The agent itself does not move. Model calls, chat, sessions, and memory all stay on the Memoh server — a connected computer only provides its filesystem and command execution, and appears to the agent as another workspace.</p><p>The connection is outbound from the computer to the server over WebSocket. The computer does not need a public IP or any open ports and can sit behind NAT.</p><div class="danger custom-block"><p class="custom-block-title">Understand what you are sharing</p><p>A connected computer exposes the files of the OS user who runs the runtime and executes commands <strong>as that user</strong>. There is no directory sandbox: any file that user can touch, a bot with access can touch. Only bots you explicitly add the computer to can use it, and write/execute actions ask for approval by default — but treat connecting a computer as granting shell access.</p></div><hr><h2 id="connecting-a-computer" tabindex="-1">Connecting A Computer <a class="header-anchor" href="#connecting-a-computer" aria-label="Permalink to &quot;Connecting A Computer&quot;">​</a></h2><h3 id="with-the-command-line" tabindex="-1">With The Command Line <a class="header-anchor" href="#with-the-command-line" aria-label="Permalink to &quot;With The Command Line&quot;">​</a></h3><ol><li>Open the <strong>Computers</strong> page in the Web UI settings.</li><li>Create a runtime key. Keys look like <code>mrk_...</code>; any signed-in user can create keys, and each computer belongs to the user who created its key.</li><li>Run the generated command on the computer you want to connect (Node.js 20+):</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --yes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @memohai/runtime</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://your-memoh-server/api</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --key</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mrk_xxxx</span></span></code></pre></div><p>The connection requires <code>wss://</code> (TLS). Plain <code>http</code> is only accepted for localhost together with <code>--insecure-localhost</code>, which the UI adds automatically when applicable. The client reconnects automatically with backoff if the connection drops.</p><p>The server URL and key can also come from the <code>MEMOH_RUNTIME_SERVER</code> and <code>MEMOH_RUNTIME_KEY</code> environment variables.</p><h3 id="from-the-desktop-app" tabindex="-1">From The Desktop App <a class="header-anchor" href="#from-the-desktop-app" aria-label="Permalink to &quot;From The Desktop App&quot;">​</a></h3><p>Memoh Desktop has this built in: enable <strong>This computer</strong>, give it a name, and the app connects its own runtime in-process — no separate terminal needed. The key is stored encrypted with the OS keychain facility, the connection is restored on restart, and the runtime stays bound to the server it was configured for (connecting the desktop app to a different server puts it into an error state instead of silently reattaching).</p><h3 id="managing-keys" tabindex="-1">Managing Keys <a class="header-anchor" href="#managing-keys" aria-label="Permalink to &quot;Managing Keys&quot;">​</a></h3><ul><li><strong>Revoking</strong> a key disconnects the computer immediately.</li><li>Keys remain readable to their owner so the connection command can be copied again later. That also means they are stored readably server-side — protect the server database accordingly.</li></ul><hr><h2 id="work-locations-on-a-bot" tabindex="-1">Work Locations On A Bot <a class="header-anchor" href="#work-locations-on-a-bot" aria-label="Permalink to &quot;Work Locations On A Bot&quot;">​</a></h2><p>Each bot has a set of <strong>Work locations</strong>:</p><ul><li><strong>Server Workspace</strong> — the bot&#39;s normal container workspace on the server. Always present, cannot be removed.</li><li>Any <strong>computers</strong> you add to the bot.</li></ul><p>Exactly one location is the <strong>default</strong>; new work happens there unless something says otherwise. Adding a computer to a bot requires bot manage permission, and a computer can only be attached to bots owned by the same user who owns the computer.</p><p>A location row shows its status: online, offline, or <strong>Update required</strong> when the computer runs an older <code>@memohai/runtime</code> client than the server needs — upgrade the client (<code>npx</code> picks up the latest automatically on restart).</p><h3 id="choosing-a-location-per-request" tabindex="-1">Choosing A Location Per Request <a class="header-anchor" href="#choosing-a-location-per-request" aria-label="Permalink to &quot;Choosing A Location Per Request&quot;">​</a></h3><ul><li>The chat composer has a <strong>computer picker</strong> to route a single message&#39;s work to a specific location.</li><li>The agent can target a location per tool call; if the requested location is unavailable, the call fails visibly — it never silently falls back to the server or another computer.</li><li>The agent can list the bot&#39;s current locations with the <code>list_execution_locations</code> tool.</li></ul><h3 id="tool-approval-per-location" tabindex="-1">Tool Approval Per Location <a class="header-anchor" href="#tool-approval-per-location" aria-label="Permalink to &quot;Tool Approval Per Location&quot;">​</a></h3><p>Every location has its own approval policy for <strong>read</strong>, <strong>write</strong>, and <strong>execute</strong>, each set to allow, ask, or deny. For computers the defaults are: read <strong>allow</strong>, write <strong>ask</strong>, execute <strong>ask</strong> — so a bot can look around, but changing files or running commands on your machine asks first. Approvals are pinned to the location they were requested on.</p><hr><h2 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h2><ul><li><strong>Browser Use / Computer Use always run in the Server Workspace</strong>, regardless of the selected location.</li><li><strong>ACP agent sessions</strong> do not support running on a computer.</li><li>Paths: the runtime works from the OS user&#39;s home directory; <code>~</code> and <code>/data</code> in tool calls are mapped to the home directory for compatibility with container workspace conventions.</li></ul><hr><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="/guides/container.html">Bot Workspace Management</a></li><li><a href="/guides/browser-computer-use.html">Browser / Computer Use</a></li><li><a href="/self-hosted/">Self-hosted Overview</a></li></ul>',32)])])}const m=t(s,[["render",r]]);export{u as __pageData,m as default};
